import 'package:flutter/material.dart';

class SmoothListItems extends StatelessWidget {
  const SmoothListItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Smooth List');
  }
}
